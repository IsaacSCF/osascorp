[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "cd org-manager && pip install -r requirements.txt"
]

[start]
cmd = "cd org-manager && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
